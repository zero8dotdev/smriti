/** `init` on the new blueprint - genuinely unimplemented (see index.ts, every branch is a TODO). */

import { BaseStubCommand } from "../command";

export class InitCommand extends BaseStubCommand {
  name = "init";
  summary = "Initialize Smriti for a project";
  detailedSummary = "Coming in Phase 1 completion. Today's CLI accepts <project-path> [--force] [--language] [--dry-run] but only prints a placeholder.";
}
